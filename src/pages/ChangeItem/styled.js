import styled from "styled-components";

const ChangeItemContainer = styled.div`
  display: flex;
`;

const CardContainer = styled.div`
  width: 100%;
  display: flex;
  justify-content: center;
`;

export { ChangeItemContainer, CardContainer };
